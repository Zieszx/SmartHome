import 'package:flutter/material.dart';

class ObjectDetectorPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Object Detector'),
      ),
      body: Center(
        child: Text('Object Detector Page'),
      ),
    );
  }
}
